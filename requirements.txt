# Core framework
fastapi>=0.115.5
uvicorn[standard]>=0.32.1
pydantic>=2.9.2
pydantic-settings>=2.6.1
# Database (psycopg3 - pure Python, no build required)
sqlalchemy[asyncio]>=2.0.36
psycopg[binary,pool]>=3.1.0
alembic>=1.14.0
# HTTP client
httpx>=0.27.2
# Redis
redis[hiredis]>=5.0.0
# LiveKit (latest compatible)
livekit>=1.0.0
livekit-agents>=1.0.0
livekit-plugins-silero>=1.0.0
# Google AI
google-generativeai>=0.8.0
# Config + YAML
python-multipart>=0.0.17
pyyaml>=6.0.2
# Security
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# Logging
structlog>=24.4.0
# Testing
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=6.0.0
# Linting (dev only)
ruff>=0.7.0
python-dotenv>=1.0.0
